driver:
  name: vagrant

provisioner:
  name: chef_zero
  product_name: chef
  product_version: latest

verifier:
  name: inspec
  inspec_tests:
    - test/integration/default

platforms:
  - name: ubuntu-18.04

suites:
  - name: default
    run_list:
      - recipe[wordpress-lamp::default]
    verifier:
      inspec_tests:
        - test/integration/default
